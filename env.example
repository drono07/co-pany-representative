# =============================================================================
# MINIMAL .env FOR FRONTEND + API PRODUCTION USE
# =============================================================================
# NOTE: When using the frontend, you only need these essential settings!
# All analysis parameters (crawl depth, page limits, etc.) come from the frontend UI.

# OpenAI API Key (Required for AI evaluation)
OPENAI_API_KEY=your_openai_api_key_here

# Database Settings (Required for data storage)
MONGODB_URI=mongodb://localhost:27017/website_analysis_platform
ENABLE_MONGODB_STORAGE=true

# =============================================================================
# OPTIONAL: CRAWLER PERFORMANCE SETTINGS
# =============================================================================
# These can be left as defaults for most use cases

# Crawler Performance Settings (optional - defaults work fine)
MAX_CRAWL_DEPTH=3
MAX_CONCURRENT_REQUESTS=10
REQUEST_TIMEOUT=30
USER_AGENT=WebsiteInsightsBot/1.0

# =============================================================================
# LOCAL TESTING ONLY (Not needed for frontend use)
# =============================================================================
# These are ONLY used when running main.py directly for testing
# Uncomment and configure only if you need to test main.py locally

# ENABLE_AI_EVALUATION=true
# MAX_AI_EVALUATION_PAGES=10
# ENABLE_LINK_VALIDATION=true
# ENABLE_BLANK_PAGE_DETECTION=true
# ENABLE_CONTENT_ANALYSIS=true
# MAX_PAGES_TO_CRAWL=500
# MAX_LINKS_TO_VALIDATE=1500
